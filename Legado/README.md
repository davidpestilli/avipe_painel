Esta pasta concentra o frontend Django antigo do AVIPE painel.

O stack atual em uso passou a ser:

- frontend: React + Vite + TypeScript + Tailwind 3.3.5
- backend: Django servindo a interface compilada e as APIs

Conteudo legado preservado:

- `Frontend_Django/templates`
- `Frontend_Django/static`

Rotas:

- nova interface: `http://127.0.0.1:8000/`
- legado: `http://127.0.0.1:8000/legado/`
